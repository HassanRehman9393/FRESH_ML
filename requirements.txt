# FRESH ML - Production Requirements (Optimized for Deployment)
# Only essential dependencies for API functionality

# Core ML Frameworks (CPU-only versions for smaller size)
torch --index-url https://download.pytorch.org/whl/cpu
torchvision --index-url https://download.pytorch.org/whl/cpu
ultralytics

# Computer Vision (minimal)
opencv-python-headless  # Headless version (smaller)
Pillow

# Essential Scientific Computing
numpy
pandas

# API Framework
fastapi
uvicorn[standard]
python-multipart

# Configuration & Cloud Storage
python-dotenv
boto3
requests
PyYAML

# Data validation
pydantic
