# FRESH ML - Absolute Minimal Production Requirements

# Core ML (CPU-only for smaller size)  
# Installed separately via pip with --index-url in Dockerfile
# torch
# torchvision
ultralytics

# Essential Libraries
numpy
Pillow
opencv-python-headless

# API Framework
fastapi
uvicorn
python-multipart

# Cloud Storage
boto3
requests
python-dotenv

# Data validation  
pydantic
